{
  "name": "docx-fix",
  "private": true,
  "version": "1.0.0",
  "description": "文档格式检查与修复工具",
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "frontend/src/**/*.{ts,tsx}": [
      "eslint --config frontend/eslint.config.js"
    ]
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.3.3"
  }
}
